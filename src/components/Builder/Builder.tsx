import Grid from '../Grid/Grid';
import './Builder.scss';

const Builder = () => {
    return (
        <div className='builder-section'>
            <Grid />
        </div>
    )
}

export default Builder